#include <stdio.h>

/**
 * main - entry point of the program
 *
 * Return: 0 on successful execution
 *
 */

int main(void)
{
	int i;
		for (i = 0; i <= 9; i++)
		{
			printf("%d", i);
		}
		putchar('\n');
		return (0);
}
